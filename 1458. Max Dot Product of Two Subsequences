//    Difficulty : HARD

//    Problem

//    Given two arrays nums1 and nums2.
//    Return the maximum dot product between non-empty subsequences of nums1 and nums2 with the same length.
//    A subsequence of a array is a new array which is formed from the original array by deleting some (can be none) of the characters without disturbing the relative positions of the remaining characters. (ie, [2,3,5] is a subsequence of [1,2,3,4,5] while [1,5,3] is not).



//    🧠 Approach (Short)

//    We use Dynamic Programming with 1D space optimization.
//    arr[j] stores the maximum dot product using nums1[0..i] and nums2[0..j-1]
//    Initialize all values with Integer.MIN_VALUE to ensure non-empty subsequences
//    For each pair (nums1[i], nums2[j]), we choose the best of:
//    Pair both elements (start new or extend previous subsequence)
//    Skip nums2[j]
//    Skip nums1[i]
//    Transition:

//    arr[j+1] = Math.max(
//        nums1[i] * nums2[j] + Math.max(0, prev),
//        Math.max(arr[j], arr[j+1])
//    );


//    prev keeps the diagonal DP value for correct extension

//    Final answer is arr[nums2.length]

//    Time: O(n × m)
//    Space: O(m)






//    Code

class Solution {
    public int maxDotProduct(int[] nums1, int[] nums2) {
       
       int arr[]=new int[nums2.length+1];
       Arrays.fill(arr,Integer.MIN_VALUE);
    
       for(int i=0;i<nums1.length;i++){
       int prev=Integer.MIN_VALUE;
        for(int j=0;j<nums2.length;j++){
            int temp=arr[j+1];
            arr[j+1]=Math.max(((nums1[i]*nums2[j])+Math.max(0,prev)),Math.max(arr[j],arr[1+j]));
            prev=temp;
        }
       }
       return arr[nums2.length];
    }
}
